export default function App() {
  return (
    <main style={{ maxWidth: 760, margin: "48px auto", padding: "0 16px" }}>
      <h1>Base Vite Lista</h1>
      <p>Estructura preparada con `app/` y `app/lib/` para iniciar el proyecto.</p>
    </main>
  );
}
